'use client';
import StaticLayout from '@/components/layout/StaticLayout';

export default function RefundPolicyPage() {
  return (
    <StaticLayout title="Refund Policy">
      <p>Sessions cancelled at least 2 hours before the scheduled time may be rescheduled.</p>
      <p>Completed sessions are non-refundable.</p>
    </StaticLayout>
  );
}
